---
- name: Restart suricata
  ansible.builtin.systemd:
    name: suricata
    state: restarted
    enabled: true
    daemon_reload: true

- name: Reload suricata
  ansible.builtin.systemd:
    name: suricata
    state: reloaded
