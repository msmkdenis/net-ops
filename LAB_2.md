## Вопрос 1: Почему важно блокировать сканирование сети?

Сканирование портов — это первый этап любой целенаправленной атаки (этап разведки).

Сокрытие поверхности атаки: Если мы блокируем сканирование, злоумышленник не узнает, какие сервисы (SSH, Web, Database) запущены и какие у них версии.

Предотвращение подбора эксплойтов: Не зная версии сервиса (например, nginx 1.18), хакер не сможет подобрать под него конкретную уязвимость (CVE).

Экономия ресурсов: Массированное сканирование (например, nmap -T5) может вызвать отказ в обслуживании (DoS) для старых сетевых устройств или слабых сервисов.

## Вопрос 2: Чем опасны техники фингерпринтинга и что такое OS Fingerprinting?

OS Fingerprinting (nmap -O) — это метод определения операционной системы удаленного хоста путем анализа особенностей реализации стека TCP/IP (как сервер отвечает на нестандартные пакеты, какие значения TTL, Window Size и IP Options он выставляет).


Опасность: Зная точную ОС (например, "Windows Server 2019" или "Linux Kernel 5.15"), атакующий отсеивает 90% неработающих эксплойтов и применяет только те, которые гарантированно пробивают эту систему (например, EternalBlue работает только на Windows). Это повышает вероятность успеха атаки.

## Вопрос 3: Какие проблемы могут возникать при избыточной блокировке? 

Основная проблема — False Positives (Ложные срабатывания).

Блокировка легитимных пользователей: Если порог (threshold) слишком низкий, активный пользователь или скрипт мониторинга (Zabbix/Prometheus) может быть принят за сканер и заблокирован.

Нарушение связности: В режиме IPS (drop) ошибочное правило разрывает соединение, что может остановить бизнес-процессы.

Сложность диагностики: Пользователь просто видит "нет сети", и админу сложно понять, что виновата IDS, без анализа логов.

## Вопрос 4: Как корректировать сигнатуры для уменьшения ложных срабатываний? 

Настройка threshold: Увеличить параметры count (количество пакетов) или уменьшить seconds (время). Например, разрешить 20 SYN-пакетов за 10 секунд вместо 10 за 6.

Белые списки (Pass rules): Добавить правила pass, которые срабатывают перед правилами alert/drop для доверенных IP-адресов (например, для сервера мониторинга).

Уточнение критериев: Добавить специфичные флаги или размеры пакетов (dsize), чтобы правило срабатывало только на утилиты сканирования, а не на браузеры.

### Мониторинг логов в консоли

```bash
sudo grep "1001001" /var/log/suricata/eve.json | jq .
```

# Отчет по Лабораторной работе №2: Suricata IPS и Nmap

В данном документе представлены финальные команды для проверки правил обнаружения и блокировки сканирования, а также подробное объяснение используемых флагов.

---

## 1. Команды для проверки (Cheatsheet)

> **Важно:** Все команды выполняются из контейнера `attacker`. IP жертвы: `172.16.90.10`.

### Тест 1: SYN Scan (Блокировка)

* **🟢 Обход (Тишина):**
    ```bash
    docker exec -it attacker nmap -sS -Pn -n --max-retries 0 -p 1-20 --scan-delay 1s 172.16.90.10
    ```
* **🔴 Триггер (Drop):**
    ```bash
    docker exec -it attacker nmap -sS -Pn -n --max-retries 0 -p 1-20 -T5 172.16.90.10
    ```
    *Проверка:* 

    ```bash
    sudo grep "1001001" /var/log/suricata/eve.json | jq .
    ```

### Тест 2: XMAS Scan (Детектирование)

* **🟢 Обход:**
    ```bash
    docker exec -it attacker nmap -sX -Pn -n --max-retries 0 -p 1-10 --scan-delay 1.5s 172.16.90.10
    ```
* **🔴 Триггер (Alert):**
    ```bash
    docker exec -it attacker nmap -sX -Pn -n --max-retries 0 -p 1-10 -T5 172.16.90.10
    ```
    *Проверка:* 

    ```bash
    sudo grep "1001002" /var/log/suricata/eve.json | jq .
    ```

### Тест 3: UDP Scan (Блокировка)

* **🟢 Обход:**
    ```bash
    docker exec -it attacker nmap -sU -Pn -n --max-retries 0 -p 1-20 --scan-delay 1.1s 172.16.90.10
    ```
* **🔴 Триггер (Drop):**
    ```bash
    docker exec -it attacker nmap -sU -Pn -n --max-retries 0 -p 1-20 -T5 172.16.90.10
    ```

    *Проверка:* 
    
    ```bash
    sudo grep "1001003" /var/log/suricata/eve.json | jq .
    ```

### Тест 4: OS Fingerprinting (Детектирование)
*Примечание:* Для гарантии сработки мы принудительно добавляем IP-опции ко всем пакетам диапазона.*

* **🟢 Обход:**
    ```bash
    docker exec -it attacker nmap -O -Pn -n --max-retries 0 --scan-delay 5s 172.16.90.10
    ```
* **🔴 Триггер (Alert):**
    ```bash
    docker exec -it attacker nmap -O -Pn -n --max-retries 0 --ip-options "R" -p 1-20 172.16.90.10
    ```
    *Проверка:* 
    
    ```bash
    sudo grep "1001101" /var/log/suricata/eve.json | jq .
    ```

### Тест 5: ACK Scan (Детектирование)

* **🟢 Обход:**
    ```bash
    docker exec -it attacker nmap -sA -Pn -n --max-retries 0 -p 1-10 --scan-delay 2.5s 172.16.90.10
    ```
* **🔴 Триггер (Alert):**
    ```bash
    docker exec -it attacker nmap -sA -Pn -n --max-retries 0 -p 1-10 -T5 172.16.90.10
    ```
    *Проверка:* 

    ```bash
    sudo grep "1001004" /var/log/suricata/eve.json | jq .
    ```

### Тест 6: FIN Scan (Детектирование)

* **🟢 Обход:**
    ```bash
    docker exec -it attacker nmap -sF -Pn -n --max-retries 0 -p 1-10 --scan-delay 4s 172.16.90.10
    ```
* **🔴 Триггер (Alert):**
    ```bash
    docker exec -it attacker nmap -sF -Pn -n --max-retries 0 -p 1-10 -T5 172.16.90.10
    ```
    *Проверка:* 

    ```bash
    sudo grep "1001005" /var/log/suricata/eve.json | jq .
    ```

### Тест 7: NULL Scan (Детектирование)

* **🟢 Обход:**
    ```bash
    docker exec -it attacker nmap -sN -Pn -n --max-retries 0 -p 1-5 --scan-delay 6s 172.16.90.10
    ```
* **🔴 Триггер (Alert):**
    ```bash
    docker exec -it attacker nmap -sN -Pn -n --max-retries 0 -p 1-5 -T5 172.16.90.10
    ```
    *Проверка:* 

    ```bash
    sudo grep "1001006" /var/log/suricata/eve.json | jq .
    ```

---

## 2. Пояснение флагов Nmap

Здесь описано, зачем нужен каждый ключ в командах выше.

### Общие флаги (Чистота эксперимента)
Эти параметры используются везде, чтобы исключить влияние посторонних факторов.

* **`-Pn` (No Ping):**
    * *Суть:* Запрещает пинговать хост перед сканированием.
    * *Зачем:* У нас есть правило `drop icmp`. Без этого флага Nmap подумает, что хост выключен, и не начнет сканирование портов.
* **`-n` (No DNS):**
    * *Суть:* Отключает разрешение имен (DNS resolution).
    * *Зачем:* Ускоряет тест и убирает "шум" (лишние UDP пакеты) из логов.
* **`--max-retries 0` (Без повторов):**
    * *Суть:* Запрещает Nmap повторять отправку пакета, если ответ не получен.
    * *Зачем:* **Критически важно** для проверки правил с `threshold`. Если мы хотим отправить ровно 10 пакетов, нам нельзя допустить, чтобы Nmap скрытно отправил еще 10 повторных.
* **`-p 1-20` (Диапазон портов):**
    * *Суть:* Сканирует только указанные порты.
    * *Зачем:* Позволяет точно контролировать количество отправленных пакетов (count). `-p 1-20` = 20 пакетов.

### Флаги скорости (Триггер vs Обход)
Именно эти параметры определяют, сработает защита или нет.

* **`-T5` (Insane Mode):**
    * *Суть:* Максимально быстрое сканирование.
    * *Эффект:* Все пакеты отправляются мгновенно. 20 пакетов улетают за 0.1 сек. Это гарантированно пробивает временное окно `seconds` в правилах Suricata.
* **`--scan-delay Xs` (Задержка):**
    * *Суть:* Ждать X секунд перед отправкой следующего пакета.
    * *Эффект:* Если правило требует "10 пакетов за 6 секунд", а мы шлем 1 пакет в секунду, то за 6 секунд накопится всего 6 пакетов. Порог не превышен -> Алерта нет.

### Специфичные флаги (Тип атаки)
Определяют, какое именно правило сигнатуры (SID) будет проверяться.

* **`-sS` (SYN Scan):** Пакеты только с флагом `SYN`. (Правило: `flags: S`).
* **`-sX` (Xmas Scan):** Пакеты с флагами `FIN, PSH, URG`. (Правило: `flags: FPU`).
* **`-sU` (UDP Scan):** Протокол UDP.
* **`-O` (OS Detection):** Запуск движка определения ОС (шлет пакеты с IP-опциями и др.).
* **`--ip-options "R"`:** Принудительное добавление опции "Record Route" в пакет. Использовалось нами, чтобы "помочь" правилу OS Fingerprinting сработать гарантированно.
* **`-sA` (ACK Scan):** Пакеты только с флагом `ACK`. (Правило: `flags: A`).
* **`-sF` (FIN Scan):** Пакеты только с флагом `FIN`. (Правило: `flags: F`).
* **`-sN` (NULL Scan):** Пакеты без флагов. (Правило: `flags: 0`).